[* Function to calculate the factorial of an integer using recursion *]
function factorial(int n)
[
    integer result;

    if (n <= 1)
    {
        return 1;  [* Base case: factorial of 0 or 1 is 1 *]
    }
    else
    {
        result = n * factorial(n - 1);  [* Recursive case: multiply by (n-1)! *]
        ret result;
    }
]

[* Function to calculate the factorial of an integer using iteration *]
function factorial(int n)
[
    integer result = 1;
    while (n > 1)
    {
        result = result * n;
        n = n - 1;
    }
    ret result;
]

[* Function to calculate the factorial of an integer using tail recursion *]